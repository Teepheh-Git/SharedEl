// @flow
import React from 'react';
import {View} from "react-native";

const GadgetsDetails = ({navigation, route}) => {
    return (
        <View>

        </View>
    );
};

export default GadgetsDetails
